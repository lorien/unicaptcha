## Report on task 10: Solver / AsyncSolver universal client

### Spec/ADR amendments

- [open] architecture.md §5 constructor sketch: `abandoned_registry_limit`
  default corrected to 1000 (owner decision — ADR-0038 wins over the
  sketch's `= None`). One-line sketch fix pending.
- [open] Universal-tier `solve` returns `TaskResult[Any]` statically
  (runtime fully typed); per-kind static binding via kind overloads was
  deferred. Facades (tasks 11-14) give precise typing naturally; if
  universal precision is wanted later, add the nine-kind overload set.

### Future-task notes

- [open] Facades (tasks 11-14): constructor parity — `api_key` positional,
  then keyword-only `base_url`, `referral` + every client kwarg except
  `adapters`; facade knows its adapter statically (no dispatch), so its
  pre-flight path is narrower (no registry lookup).
- [open] `check_sync_handler(what=...)` labels: sync tier validates at two
  attachment sites (constructor + per-call) with distinct labels — keep
  that pattern in facades.
- [open] Proxy application currently triggers a WARNING only on solve-path
  dispatch; facade convenience methods must route through the same
  `_internal/routing.apply_default_proxy` to inherit the behavior.

### Tooling/process

- [acted] Pre-flight provider hints flow from routing to client events via
  an `on_pre_flight(provider_hint, error_kind, message)` callback invoked
  immediately before each raise; when no provider is resolvable the event
  is skipped entirely (honest emission beats placeholder values).
- [acted] Random selection is pinned in tests by monkeypatching
  `_internal.routing.uniform_choice` (the ADR-0064 injectable hook).
- [acted] pyright's modern typeshed types `dataclasses.replace` strictly
  against declared fields; dynamic field additions need explicit casts
  (`_internal/routing.apply_default_proxy`).
- [acted] Two-generation test adapters (an intermediate base + per-provider
  subclasses) collide with `__init_subclass__` required-declaration checks
  unless the intermediate carries placeholders — pattern documented in
  tests/test_client.py for task 15's reference adapter.