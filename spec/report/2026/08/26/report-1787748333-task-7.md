## Report on task 7: Adapter SDK contract

### Spec/ADR amendments

- [acted] ADR-0053 signature tweak: `referral` is keyword-only (owner
  decision during this task) — the ADR sketch shows
  positional-or-keyword; aligned the ADR's `__init__` signature with `*`
  (spec sweep 2026-08-26).
- [acted] ADR-0036 naming rule: `adapter.py` (singular) was chosen by the
  owner over the root-files-plural convention ("one base adapter class").
  Recorded as a single-concern-file exception (spec sweep 2026-08-26).
- [acted] `map_provider_error` return shape `tuple[ErrorKind, str]` was
  asserted from architecture's "-> ErrorKind and message" and confirmed
  by the owner in-session; pinned in architecture.md (spec sweep
  2026-08-26).

### Future-task notes

- [open] Registration check (non-adapters in `adapters=` -> `TypeError`)
  lands with the universal clients (tasks 9/10): one
  `isinstance(adapter, BaseAdapter)` per element.
- [open] Shipped adapters (tasks 11-14) must declare `__slots__` on their
  additional attributes — slotscheck scans `unicaptcha` and BaseAdapter
  has `__slots__`, so subclasses without it would be flagged (and would
  add a `__dict__`).
- [open] The full "reference third-party adapter never imports `_internal`"
  CI check belongs to task 15; this task pinned only that the SDK module
  (`adapter.py`) itself has no `_internal` imports.
- [open] `default_task_config` typed `Mapping[type[BaseChallenge],
  TimeConfig] | None`; the engine (task 9) defines the exact consumption
  shape of the per-kind timing table.

### Tooling/process

- [acted] The isolation test initially over-scoped (asserted the whole
  public surface has no `_internal` imports) and failed because
  `types.py`/`challenge/`/`solution/` legitimately import
  `_internal.repr`/`taxonomy`. ADR-0041's rule is about the *reference
  adapter*, not public modules; corrected to scan `adapter.py` only. ADR-0041
  amended to note that public modules may consume `_internal` helpers
  (only third-party adapter code may not) (spec sweep 2026-08-26).
- [acted] `__init_subclass__` enforcement gives fail-early, named errors
  for missing required ClassVars (provider/challenges/default_base_url) —
  cleaner than an `AttributeError` at registration time.