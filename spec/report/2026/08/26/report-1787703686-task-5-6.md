## Report on task 5: Challenge kind bases (with task 6: Solution kind bases)

### Spec/ADR amendments

- [acted] Note in the spec/plan that tasks 5+6 are one unit: the
  challenge->solution type link and the symmetric taxonomy make them
  inseparable (executed folded, owner-approved; noted in task-5/task-6,
  spec sweep 2026-08-26).

### Future-task notes

- [open] Adapter tasks (11-14): confirm GeeTest v4 `gen_time` is a `str`
  on every provider; if any returns int/float it is a per-provider
  subclass adjustment, not a base change. All five GeeTest v4 answer
  fields are currently required (no None defaults).
- [open] Provider extras and `proxy`/`user_agent`/`cookies` land on the
  provider subclasses in tasks 11-14 (ADR-0012/0069/0076), deliberately
  absent from the kind bases; the `solution_type` link and kw_only field
  pattern are what those tasks build on.

### Tooling/process

- [acted] CPython bug: `super().__post_init__()` inside a `slots=True`
  dataclass `__post_init__` raises `TypeError` — kind bases must not
  chain the root guard via `super()`; each solution kind calls
  `guard_abstract(self, <Kind>)` directly (promoted into bootstrap.md in
  the report-process task).
- [acted] pyright `reportPrivateUsage` forced the shared guard into
  `_internal/taxonomy.py` — the canonical home for shared abstract-base
  machinery.
- [acted] mypy narrowing in `ImageChallenge`: a separate `path` variable
  in the `Path` branch and `cast(bytes, self.body)` in `__repr__` encode
  the documented "always stores bytes" contract.
- [acted] Root `__all__` grew by 20 kind-base names; RUF022 sort +
  root-export test keep it mechanically consistent.