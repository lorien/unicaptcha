## Report on task 1: Toolchain + package scaffold

### Spec/ADR amendments

- [acted] ADR-0019: record that `[tool.ruff.format]` excludes `*.md` /
  `*.markdown` (ruff 0.11+ would reformat the code blocks inside
  ADR/README markdown) and that both type checkers are scoped to the
  `unicaptcha` package only (spec sweep 2026-08-26).
- [acted] testing.md: make the checkers' package-scope explicit in the
  documented commands (`uv run pyright unicaptcha` == `uv run mypy
  unicaptcha` scope) (spec sweep 2026-08-26).

### Future-task notes

- [acted] Coverage in CI (informational flags / any future threshold) is
  recorded as deferred.md #21; task 18 owns the release-consistency tag
  job. Nothing further here.
- [open] The CI 3.14t informational job relies on setup-uv supporting the
  `t` suffix; `continue-on-error` keeps it non-blocking if install flakes.

### Tooling/process

- [acted] pytest exits 4 when `testpaths = ["tests"]` points at a missing
  directory — a `tests/` dir with at least one collectable test is a
  baseline prerequisite (promoted into testing.md in the report-process
  task).
- [acted] Bare `pyright` scans the gitignored `var/` competitor checkouts;
  `[tool.pyright] include = ["unicaptcha"]` scopes it (promoted into
  testing.md in the report-process task).