## Report on task 3: Error hierarchy
State: new

### Spec/ADR amendments

- [open] Consider keeping the 1:1 `ErrorKind`-to-class table in one
  shared module so adapter tests (task 17) reuse it rather than
  re-declaring the mapping.

### Future-task notes

- [open] Behavioral requirements are not encodable in the classes and must
  be tested in tasks 7-10: `raise ... from cause` chaining, wrong-provider
  `TypeError` pre-flight (ADR-0045), malformed-response `ProviderError`
  with `__cause__` (ADR-0040).

### Tooling/process

- [acted] Exception pickling is a deliberate non-goal: `raw_response` is
  keyword-only state that `Exception.__reduce__` would drop; no ADR
  requires picklable errors.
- [acted] `EmptySolutionError.__init__` calls `UnicaptchaError.__init__`
  directly to bypass `ProviderError`'s hardcoded kind while staying a
  `ProviderError` subclass; explained in the docstring.
- [acted] Root `__all__` growth is guarded by the RUF022 sort + the
  root-export test; nothing further.