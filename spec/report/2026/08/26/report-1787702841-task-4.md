## Report on task 4: Events
State: new

### Spec/ADR amendments

- [open] architecture.md: record the `TaskEvent` field-default decision —
  `elapsed`/`attempt` required, `task_id`/`detail`/`error_kind` default
  `None` (optional per the matrix). Revisit only if the engine wants a
  fully-optional event shape.

### Future-task notes

- [open] The `error_kind` matrix (currently only in test_events.py) should
  be shared/imported by the engine (task 9) and adapters (tasks 11-14) so
  documented matrix and reality cannot drift.
- [open] Clients (tasks 9/10) must consume the `_internal/handlers.py`
  machinery: call `check_sync_handler` for constructor and per-call
  handlers on the sync tier; use the flat `unicaptcha` logger for the
  awaitable-discard WARNING.

### Tooling/process

- [acted] pytest-asyncio strict-mode gotcha: an `async def` test needs
  `@pytest.mark.asyncio` (two guard tests failed collection and were made
  sync) — promoted into testing.md in the report-process task.
- [acted] The awaitable-discard WARNING path is tested with an `__await__`
  stub rather than a real coroutine, avoiding asyncio's "never awaited"
  RuntimeWarning; detection is identical (`inspect.isawaitable`).
- [open] Consider a shared `_internal/log.py` for the flat `unicaptcha`
  logger if logging call sites proliferate (first use landed in
  `_internal/handlers.py`).