## Report on task 15: Test scaffold + reference third-party adapter

### Spec/ADR amendments

- [acted] ADR-0046 reference adapter is now a real artifact: `tests/_myservice.py`
  implements the full public SDK contract (instant fast path, report pairs,
  per-kind timing, error mapping) and registers into real Solver/AsyncSolver
  in `tests/test_reference_adapter.py`. The `_internal`-import guard is an
  AST-based pytest test, so it runs in CI automatically (ADR-0046's "CI
  asserts") without a ci.yml edit.
- [acted] Coverage config added (`[tool.coverage.run]`/`[tool.coverage.report]`,
  informational only — no gate, respecting ADR-0047). Not wired into pytest
  `addopts`; `uv run pytest --cov` reports ~84%.

### Future-task notes

- Task 16 (engine injectable-clock tests) can now lean on `conftest.py`
  fixtures (`fast_time`/`fast_retry`) instead of per-file helpers.
- Task 17 (adapter golden-payload tests): fixture sourcing must derive from
  vendor sources (deferred item 22); the reference adapter demonstrates the
  shape for golden fixtures.
- `tests/_fake.py` FakeAdapter (provider "myservice") and
  `test_engine.py`'s ScriptedAdapter now coexist with the reference adapter
  under the same provider name — no collisions (different modules, never
  co-registered), but consolidating onto `_myservice.py` later would reduce
  triple duplication.

### Tooling/process

- Raising mapped errors from a public adapter requires constructing the
  public exception classes directly (`cls(message, raw_response=raw)`) —
  the reference adapter documents this (external authors cannot use
  `_internal.errors.error_from_kind`).
- The report-good/report-bad endpoints differ (`/reportCorrect` vs
  `/reportIncorrect`) — a test that mock-only `/reportIncorrect` caught the
  mismatch; worth a note for the golden tests.