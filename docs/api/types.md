# Types

The public model vocabulary: results, statuses, task addressing, proxy,
secret strings, and the three config types. All are frozen dataclasses
re-exported from the package root.

::: unicaptcha.types