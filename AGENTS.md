## Important Things

- When the model needs to ask the user something, it MUST NOT use a
  predefined-options/dialog mechanism. It may describe any options in plain
  text only, and it must expect the user's answer as plain text. It must
  never require the user to click or select a predefined option.
